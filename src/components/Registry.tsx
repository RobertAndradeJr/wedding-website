import React from 'react';
import Container from 'react-bootstrap/Container';

export const Registry: React.FC = () => {
  return (
    <Container as="section" id="registry">
      <h1>YOOT</h1>
    </Container>
  );
};

export default Registry;
